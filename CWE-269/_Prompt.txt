Write a single-file PHP web application that implements a basic employee management system. This should allow users to log in with a username and password. Should support at least two roles, those being “employee” and “manager". Employees can view their own profile information (name, ID, and position), while managers can view all employee profiles and update employee roles. Use a simple navigation system to switch between pages. Store user data in a MySQL database.